# 2026-05-01

## Git History Cleanup — COMPLETED

- Cleaned git history using `git filter-repo` replacing secrets with `[REDACTED]`
- Force-pushed to GitHub (commit `a8882f5` → `2746332`)
- Backup at `/Users/ray/Work/toolblip-backup-*`

## Neon PostgreSQL Password Rotation — COMPLETED

- Old exposed password: `[REDACTED]` (from Apr 29 commit bda263b5)
- New password: `[REDACTED]` (rotated via browser automation)
- API key generated: `[REDACTED]`
- Updated `.env` files:
  - `/Users/ray/Work/toolblip/.env`
  - `/Users/ray/Work/toolblip/api/.env`

## Stripe Staging Mode Feature — COMPLETED (backend)

Added to `api/`:
- `app/Helpers/StripeHelper.php` — picks test vs prod keys based on staging mode
- `app/Http/Middleware/EnsureStripeStagingMode.php` — blocks non-admins when staging on
- `config/services.php` — added `test_secret`, `test_webhook_secret`, `staging_mode`
- `routes/api.php` — added `GET /api/stripe/status` and middleware on checkout
- `.env` — test keys and `STRIPE_STAGING_MODE=false`

Test keys set (actual values in api/.env, not in git)

Pushed to GitHub (`411ea92`), Railway token expired - needs manual deployment.

## Gitignore Updated — COMPLETED

Added explicit OpenClaw protection:
```
.claude-home/
.openclaw/
```

## Directory Page — Already Done

`page.tsx` — thin Next.js wrapper with metadata + Suspense
`DirectoryClient.tsx` — full client implementation (search, categories, cards, scroll)

## Railway Token - May 1, 2026

- New Railway token received: `[REDACTED]`
- Saved to: `/Users/ray/Work/toolblip/api/.env`
- Note: Railway API returns 404 on all endpoints with this token - may need to verify token format or regenerate from Railway dashboard
- Railway project ID: `fbf89789-2505-4d49-8f00-3cfeb9fe11fe`
- Railway environment: `970533e1-82a4-4efc-a6ac-61c03d1cf53c`
# 2026-05-01

## Git History Cleanup — COMPLETED

- Cleaned git history using `git filter-repo` replacing secrets with `[REDACTED]`
- Force-pushed to GitHub (commit `a8882f5` → `2746332`)
- Backup at `/Users/ray/Work/toolblip-backup-*`

## Neon PostgreSQL Password Rotation — COMPLETED

- Old exposed password: `[REDACTED]` (from Apr 29 commit bda263b5)
- New password: `[REDACTED]` (rotated via browser automation)
- API key generated: `[REDACTED]`
- Updated `.env` files:
  - `/Users/ray/Work/toolblip/.env`
  - `/Users/ray/Work/toolblip/api/.env`

## Stripe Staging Mode Feature — COMPLETED (backend)

Added to `api/`:
- `app/Helpers/StripeHelper.php` — picks test vs prod keys based on staging mode
- `app/Http/Middleware/EnsureStripeStagingMode.php` — blocks non-admins when staging on
- `config/services.php` — added `test_secret`, `test_webhook_secret`, `staging_mode`
- `routes/api.php` — added `GET /api/stripe/status` and middleware on checkout
- `.env` — test keys and `STRIPE_STAGING_MODE=false`

Test keys set (actual values in api/.env, not in git)

Pushed to GitHub (`411ea92`), Railway token expired - needs manual deployment.

## Gitignore Updated — COMPLETED

Added explicit OpenClaw protection:
```
.claude-home/
.openclaw/
```

## Directory Page — Already Done

`page.tsx` — thin Next.js wrapper with metadata + Suspense
`DirectoryClient.tsx` — full client implementation (search, categories, cards, scroll)

## Railway Token - May 1, 2026

- New Railway token received: `[REDACTED]`
- Saved to: `/Users/ray/Work/toolblip/api/.env`
- Note: Railway API returns 404 on all endpoints with this token - may need to verify token format or regenerate from Railway dashboard
- Railway project ID: `fbf89789-2505-4d49-8f00-3cfeb9fe11fe`
- Railway environment: `970533e1-82a4-4efc-a6ac-61c03d1cf53c`

## Railway Token v2 - May 1, 2026 (afternoon)

- New PAT received: `4f89be0a-4172-482b-b1cc-aff0b833d950`
- Updated in `/Users/ray/Work/toolblip/api/.env`
- **Important:** Railway CLI v4.37.2 uses OAuth login flow (`railway login -b`), not PAT. Headless use not possible.
- All Railway commands require interactive browser authentication
- Workaround: Manual Railway dashboard deployments, or user runs `railway login` locally with this token

## Railway API Access - SUCCESS (GraphQL)

- Found correct endpoint: `https://backboard.railway.app/graphql/v2`
- Token works with GraphQL API
- Created `railway.sh` script: `./railway.sh '{"query":"..."}'`
- Email confirmed: harun.b13@gmail.com

## Railway Deployment Issues

- API service `e39d0cb5-c516-4fb5-9b56-9ae3b7b39ad2` has NO source configured
- GitHub app not installed on `toolblip-api` repo
- Deployment via `githubRepoDeploy` fails: "repo is either not public or doesn't exist"
- Set `rootDirectory: "/api"` on service instance
- All manual deploys fail immediately (no logs captured)

**Fix needed:** Install Railway GitHub app on `toolblip-api` repo, or connect via Railway dashboard.

## Railway Deployment - WORKING

- Successfully deployed API via Railway GraphQL API
- Service ID: `e39d0cb5-c516-4fb5-9b56-9ae3b7b39ad2`
- Deployment confirmed: `bd40fae4-d2ad-4fb5-baf8-3aef9e90918c` (SUCCESS)
- API responding at: https://api.toolblip.com/api/stripe/status → `{"staging_mode":false}`
- Start command: `bash start.sh` (uses PHP built-in server)
- Builder: RAILPACK
- Source: GitHub `toolblip/toolblip-api` (made public temporarily)

## Missing on Railway

API environment variables needed:
- APP_KEY (generate with `php artisan key:generate`)
- STRIPE_SECRET (actual production key)
- STRIPE_KEY (actual publishable key)
- Other app-specific vars

Fix: User needs to set these in Railway dashboard → project → API service → variables.


## Stripe Staging Mode — Frontend (May 1, 2026 - Afternoon)

### Created
- `src/lib/stripeStatus.ts` — client hook fetching `GET /api/stripe/status`
- `src/components/StripeStagingBanner.tsx` — fixed bottom banner, amber/warning style, dismissible
- `src/app/donate/page.tsx` — refactored to client component
- `src/app/donate/DonateClient.tsx` — full donate UI with Stripe checkout, preset amounts ($5-$100), custom amount, test mode notice

### Backend (already done earlier)
- `EnsureStripeStagingMode` middleware — blocks non-admins at `/api/subscription/checkout` when staging on
- `StripeHelper` — picks test vs prod keys based on `STRIPE_STAGING_MODE` env var
- `GET /api/stripe/status` — returns `{staging_mode: bool}`

### Railway GitHub Auto-Deploy — BLOCKED
- Railway GitHub App not installed on `toolblip` org
- Both frontend and API repos are private → Railway can't pull code
- Deploys fail with "error generating deployment: repository not accessible"
- Workaround: Use `deploymentRedeploy` mutation on Railway API (works fine)
- Fix: User needs to install Railway GitHub app on org (pending)

### API Env Vars Still Needed on Railway
- APP_KEY, STRIPE_SECRET, STRIPE_KEY, etc. not set → `/api/plans` errors
- Railway deployment otherwise working via redeploy workaround
