# memory/2026-04-12.md

## Session Start

- Started fresh. Read SOUL.md, checked today's date.
- Ray asked about connecting the OpenClawAir topic 6 to the crontinel %ct project agent.

## What Happened

- Ray wanted messages in topic 6 of OpenClawAir to always route to the `ct` agent without having to mention `%ct`.
- The group chat ID is `-1003535215877`.
- The `tb` agent previously had the binding for this group.
- **Unbound** `tb` from `telegram:-1003535215877`, then **bound** `ct` to the same.
- Final routing: `ct <- telegram accountId=-1003535215877`
- This means from now on, messages in topic 6 of OpenClawAir will be handled by the `ct` agent (crontinel project).
- The old `tb` agent binding for the group was replaced.

## Notes

- The group has topic ID `6` — but routing appears to be at the group/account level, not per-topic.
- When Ray said "this topic" he meant topic 6 of the group. Routing in OpenClaw seems to be group-level, not topic-level. So all topics in the group route to `ct` now.
- The `tb` agent is still present and functional, just no longer bound to this specific group chat.
